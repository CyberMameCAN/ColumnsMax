[project]
name = "ColumnsMax"
version = "0.1.0"
description = ""
authors = [
  {name = "Toshiya Takeshita", email = "toshi@atbat.jp"}
]
dependencies = [
  "pygame>=2.5.2",
]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
  "pytest>=5.2",
]

[tool.poetry]
name = "ColumnsMax"
version = "0.1.0"
description = ""
authors = ["Toshiya Takeshita <toshi@atbat.jp>"]

[tool.poetry.dependencies]
python = "^3.10"
pygame = "^2.5.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
